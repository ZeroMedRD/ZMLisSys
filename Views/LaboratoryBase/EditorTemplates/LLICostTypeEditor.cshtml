@using System.Collections
@using Kendo.Mvc.UI

@(Html.Kendo().DropDownListFor(m => m)
        .Name("LLICostType")
        .DataValueField("CBDRowid")
        .DataTextField("CBDDescription")
        .SelectedIndex(0)
        .DataSource(ds => ds
            .Read(read => read.Action("GetCombo", "GetData", new { sText = "LLICostType" }))
        )
        .Filter("contains")
 )
