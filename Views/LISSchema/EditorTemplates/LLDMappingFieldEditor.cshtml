@using System.Collections
@using Kendo.Mvc.UI

@(Html.Kendo().DropDownListFor(m => m)
    .Name("LLDMappingField")
    .DataTextField("CBDDescription")
    .DataValueField("CBDRowid")
    .AutoWidth(true)
    .ValuePrimitive(true)
    .Filter("contains")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetCombo", "GetData", new { stext = "ZMLisSys_LLDMappingField" });
        }).ServerFiltering(false);
    }).Enable(true).HtmlAttributes(new { @class= "formfont" }))